services:
  app:
    build: .
    container_name: chat-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:8080"
    networks:
      - chat-net

volumes:
  caddy_data:
  caddy_config:

networks:
  chat-net:
    driver: bridge
